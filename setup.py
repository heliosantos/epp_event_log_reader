from setuptools import setup, find_packages

setup(
    name='epp_event_log_reader',
    version='1.0',
    description='A library to parse .event files from Eggplant Performance test results.',
    packages=find_packages(),
)
